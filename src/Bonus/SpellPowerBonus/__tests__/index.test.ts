import { SpellPowerBonus, SpellPowerKeys } from '../';

describe('index', () => {
    it('should export SpellPowerBonus', () => {
        expect(SpellPowerBonus).toBeDefined();
    });

    it('should export SpellPowerKeys', () => {
        expect(SpellPowerKeys).toBeDefined();
    });
});
