import { BaseSpeedsBonus, SpeedsModifiersBonus } from '../';

describe('index', () => {
    it('should export BaseSpeedsBonus', () => {
        expect(BaseSpeedsBonus).toBeDefined();
    });

    it('should export SpeedsModifiersBonus', () => {
        expect(SpeedsModifiersBonus).toBeDefined();
    });
});
