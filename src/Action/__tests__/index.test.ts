import { ActionTime } from '..';

describe('Action', () => {
    describe('ActionTime', () => {
        it('should export ActionTime', () => {
            expect(ActionTime).toBeDefined();
        });
    });
});
