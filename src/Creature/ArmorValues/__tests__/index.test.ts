import { ArmorValues, FullArmorValues } from '../index';

describe('index', () => {
    it('should export ArmorValues', () => {
        expect(ArmorValues).toBeDefined();
    });

    it('should export FullArmorValues', () => {
        expect(FullArmorValues).toBeDefined();
    });
});
